import styled from "styled-components";

const LoadingContainer = styled.div`
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
`;

const EndDataContext = styled.div`
  text-align: center;
`;

const S = { LoadingContainer, EndDataContext };

export default S;
